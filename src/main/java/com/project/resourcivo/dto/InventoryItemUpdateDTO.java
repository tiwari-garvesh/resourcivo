package com.project.resourcivo.dto;

public class InventoryItemUpdateDTO {
    // InventoryItem entity is empty except for ID, so this DTO is minimal
    // Add fields as needed when the entity is properly defined
}
